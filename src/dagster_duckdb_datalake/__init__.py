"""Package entrypoint for the Dagster + DuckDB data lake learning project.

What this file is:
	The root Python package marker for `dagster_duckdb_datalake`.

Why it is relevant:
	It defines the importable package boundary so Dagster and tests can import
	modules like `definitions` and `duckpond` reliably.
"""


